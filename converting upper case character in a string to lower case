include <stdio.h>

int main() {
    char ch1[20];
    int i,n,temp,j;
    scanf("%[^\n]s",ch1);
    for(i=0;ch1[i]!='\0';i++)
    {
        if(ch1[i]>=65&&ch1[i]<=90)
        {
            ch1[i]=ch1[i]+32;
        }
        }
        printf("%s",ch1);
    return 0;
}
